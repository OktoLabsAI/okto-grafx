"""Tests for C4, the write-ahead log.

This directory is a package so that a test module can import the doubles that live in its own
conftest without depending on which directory pytest happened to put on the import path.
"""
